name: Publish image

on:
  push:
    tags: ["v*"]

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  publish:
    uses: groovemap-music/.github/.github/workflows/reusable-image-release.yml@main
    with:
      image-name: operations-console
      requires-private-library: true
      prepare-command: just prepare-runtime-wheel
    secrets: inherit
