[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[project]
name = "groovemap-operations-console"
version = "0.1.0"
description = "Privileged administration and monitoring console for GrooveMap"
authors = [{name = "Robert Wlodarczyk", email = "robert@simplicityguy.com"}]
readme = "README.md"
requires-python = ">=3.14,<3.15"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: Other/Proprietary License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.14",
]
license = "PolyForm-Noncommercial-1.0.0"
license-files = ["LICENSE"]
dependencies = [
    "aio-pika>=10.0.1",
    "fastapi>=0.141.1",
    "groovemap-runtime[neo4j,postgres,rabbitmq]==0.1.0",
    "httpx>=0.28.1",
    "neo4j-rust-ext>=6.2.0.0",
    "orjson>=3.12.0",
    "pika>=1.4.4",
    "prometheus-client>=0.26.0",
    "psycopg[binary]>=3.3.4",
    "pydantic>=2.13.4",
    "python-multipart>=0.0.32",
    "structlog>=26.1.0",
    "uvicorn[standard]>=0.52.4",
    "websockets>=17.0.1",
]

[project.scripts]
operations-console = "dashboard.dashboard:main"

[project.urls]
Homepage = "https://groovemap.music"
Repository = "https://github.com/groovemap-music/operations-console"
Issues = "https://github.com/groovemap-music/operations-console/issues"

[dependency-groups]
dev = [
    "build>=1.3.0",
    "commitizen>=4.18.0",
    "cyclonedx-bom>=7.3.1",
    "mypy>=2.3.1",
    "pip-audit>=2.10.1",
    "pip-licenses>=5.5.5",
    "playwright>=1.62.0",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "pytest-base-url>=2.1.0",
    "pytest-cov>=7.1.0",
    "pytest-playwright>=0.9.0",
    "pytest-timeout>=2.4.0",
    "pytest-xdist>=3.8.0",
    "ruff>=0.16.4",
]

[tool.hatch.build.targets.wheel]
packages = ["dashboard"]

[tool.hatch.build]
artifacts = ["dashboard/static/tailwind.css"]

[tool.uv]
exclude-newer = "2 days"

[tool.uv.sources]
groovemap-runtime = { git = "https://github.com/groovemap-music/python-libraries.git", rev = "28fa329702bc76896cc54ab8d05ec5b1bd3d929e" }

[tool.ruff]
extend-exclude = [
    "dashboard/catalog_contract.py",
    "dashboard/catalog_admin_contract.py",
    "docs/superpowers",
]
line-length = 150
target-version = "py314"

[tool.ruff.lint]
select = ["ARG", "B", "C4", "E", "F", "I", "PLC", "PTH", "RUF", "S", "SIM", "T20", "TCH", "UP", "W"]
ignore = ["B008", "C901", "E501", "S101"]

[tool.ruff.lint.isort]
known-first-party = ["dashboard"]
lines-after-imports = 2

[tool.ruff.lint.per-file-ignores]
"dashboard/dashboard.py" = ["T201"]
"tests/**/*.py" = ["ARG", "PLC", "S105", "S106", "T201"]
"scripts/*.py" = ["S108", "T201"]

[tool.mypy]
python_version = "3.14"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true
packages = ["dashboard"]

[[tool.mypy.overrides]]
module = ["aio_pika.*", "neo4j.*", "pika.*", "prometheus_client.*", "psycopg.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
addopts = "-ra -q --strict-markers --tb=short"
testpaths = ["tests"]
asyncio_mode = "strict"
asyncio_default_fixture_loop_scope = "function"
timeout = 60
timeout_method = "thread"
markers = [
    "benchmark: performance regression benchmarks",
    "e2e: browser-based end-to-end tests",
    "e2e: end-to-end tests requiring external services",
    "integration: tests requiring a live backing service",
]

[tool.coverage.run]
source = ["dashboard"]
omit = ["*/tests/*", "*/__init__.py"]

[tool.coverage.report]
exclude_lines = [
    "def __repr__",
    "if __name__ == .__main__.:",
    "pragma: no cover",
    "raise AssertionError",
    "raise NotImplementedError",
]

[tool.commitizen]
name = "cz_conventional_commits"
version_provider = "uv"
version_scheme = "pep440"
version_files = ["dashboard/__init__.py:__version__"]
tag_format = "v$version"
annotated_tag = true
update_changelog_on_bump = true
changelog_file = "CHANGELOG.md"
